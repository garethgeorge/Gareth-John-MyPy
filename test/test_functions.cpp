/*#include "../lib/catch.hpp"

#include "include/test_helpers.hpp"

TEST_CASE("should be able to call a function", "[functions]") {
    SECTION( "without default args" ){

    }
    SECTION( "with default args" ){
        
    }
    SECTION( "from another function" ){
        
    }
    SECTION( "recursively" ){
        
    }

    // DONT FORGET TO TEST FUNCTION PERFORMANCE
    //one experiment I’d be interested to see is
     if you could run a benchmark of calling some function a
      million times in a loop, and then do the same in python
}

TEST_CASE("should error ", "[functions]") {
    SECTION( "with all required arms" )
}*/